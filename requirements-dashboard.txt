# Dashboard-specific requirements
# Install with: pip install -r requirements-dashboard.txt

# Core Dash dependencies
dash>=2.14.0
dash-bootstrap-components>=1.5.0

# Already have pandas from main requirements
# pandas>=2.0.0

# Optional: Production server
gunicorn>=21.2.0  # For production deployment on Linux/Mac
waitress>=2.1.2   # For production deployment on Windows

# Optional: Progress tracking
redis>=5.0.0      # If implementing real-time progress with Redis
celery>=5.3.0     # If implementing background task queue
